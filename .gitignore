node_modules/
dist/
.turbo/
*.tsbuildinfo
.DS_Store
.idea/
test-results*/
.claude/
.mockups/
.superpowers/
.gsd/

# Build-only manifests written transiently by the controller.
mewa-code/apps/controller/src/*.generated.ts
# Scratch files `bun build --compile` leaves behind when a binary build is interrupted
*.bun-build

# Local env / secrets (Vite auto-loads .env*; app handles provider auth tokens)
.env
.env.local
.env.*.local
