node_modules/
dist/
.turbo/
*.tsbuildinfo
.DS_Store
.idea/
test-results/
playwright-report/
blob-report/
playwright/.cache/
e2e/screenshots/
# Workflow-test run log is local evidence, not a committed record.
e2e/.workflow-runs.jsonl

.playwright-mcp/
.claude/
.mockups/
.superpowers/
.gsd/

# Build-only manifests written transiently by apps/cli/scripts/build-binary.ts
apps/cli/src/*.generated.ts
# Scratch files `bun build --compile` leaves behind when a binary build is interrupted
*.bun-build

# Local env / secrets (Vite auto-loads .env*; app handles provider auth tokens)
.env
.env.local
.env.*.local
test-results-binary/
playwright-report-binary/

# Astro's generated type cache
.astro/
